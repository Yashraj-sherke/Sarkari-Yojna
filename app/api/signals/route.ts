import {checkOrigin,readBody,db,failure,json,rateLimit,getScheme,HttpError,session} from '@/lib/server';
import {z} from 'zod';
export async function POST(req:Request){try{checkOrigin(req);await rateLimit(req,'signals');const {slug}=z.object({slug:z.string().max(100)}).parse(await readBody(req));if(!await getScheme(slug))throw new HttpError(404,'योजना नहीं मिली।');const s=(await session(req,true))!;await db().prepare('INSERT OR IGNORE INTO signals VALUES (?,?)').bind(s.id,slug).run();const count=await db().prepare('SELECT count(*) AS n FROM signals WHERE slug=?').bind(slug).first<{n:number}>();return json({count:count?.n??0},200,s.cookie);}catch(e){return failure(e);}}
